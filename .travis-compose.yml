version: "2"
services:
  db:
    build: https://github.com/Connexions/cnx-db.git#all-current-view
    ports:
      - "5432:5432"
    environment:
      - DB_USER=cnxarchive
      - DB_NAME=cnxarchive-testing
